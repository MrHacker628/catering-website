# Copy this file to .env and fill in real values — never commit .env.
# All values below are placeholders only.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=catering_db
PORT=5000

RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

EMAIL_USER=
EMAIL_PASS=

GOOGLE_MAPS_KEY=

# Admin dashboard login password (used by /auth/admin-login)
ADMIN_PASSWORD=
