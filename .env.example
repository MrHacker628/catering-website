# Copy this file to .env and fill in a real token — never commit .env.
# Used only by importToSanity.js (run with: node --env-file=.env importToSanity.js)
# Create a token at https://sanity.io/manage — project "4wdchewz" → API → Tokens.
SANITY_WRITE_TOKEN=
